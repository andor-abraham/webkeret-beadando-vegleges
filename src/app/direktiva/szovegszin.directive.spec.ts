import { Component } from '@angular/core';
import { SzovegszinDirective } from './szovegszin.directive';

describe('SzovegszinDirective', () => {
  it('should create an instance', () => {
    expect(Component).toBeTruthy();
  });
});
