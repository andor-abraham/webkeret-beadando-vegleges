import { SorszinezesDirective } from './sorszinezes.directive';

describe('SorszinezesDirective', () => {
  it('should create an instance', () => {
    const directive = new SorszinezesDirective();
    expect(directive).toBeTruthy();
  });
});
